# Optional: the Sanity project ID and dataset are already set as defaults in
# src/sanity/env.ts. Copy this file to .env.local only if you need to point
# at a different project or dataset.
NEXT_PUBLIC_SANITY_PROJECT_ID=alsdve2t
NEXT_PUBLIC_SANITY_DATASET=production
